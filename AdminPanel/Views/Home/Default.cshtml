<cache expires-sliding="@TimeSpan.FromMinutes(5)">
    <html-extraction html-local-path="@System.IO.Path.Combine("wwwroot","dist","pages","index.html")"></html-extraction>
</cache>

@ViewData["userName"]

 @section Scripts {
    <!-- This is needed for non partial rendering -->
}
